IF(WIN32)
	IF(MSVC)
		IF((CMAKE_GENERATOR_PLATFORM STREQUAL "x64") OR (CMAKE_GENERATOR MATCHES "Win64"))
			SET(LOO_ARCH_NAME "x64")
			SET(LOO_VS_PLATFORM_NAME "x64")
		ELSEIF((CMAKE_GENERATOR_PLATFORM STREQUAL "ARM64") OR (CMAKE_GENERATOR MATCHES "ARM64"))
			SET(LOO_ARCH_NAME "arm64")
			SET(LOO_VS_PLATFORM_NAME "ARM64")
		ELSEIF((CMAKE_GENERATOR_PLATFORM STREQUAL "ARM") OR (CMAKE_GENERATOR MATCHES "ARM"))
			SET(LOO_ARCH_NAME "arm")
			SET(LOO_VS_PLATFORM_NAME "ARM")
		ELSE()
			MESSAGE(FATAL_ERROR "This CPU architecture is not supported ${CMAKE_GENERATOR_PLATFORM}")
		ENDIF()
	ENDIF()
	IF(WINDOWS_STORE)
		SET(LOO_PLATFORM_NAME "win_store")
		SET(LOO_PLATFORM_WINDOWS_STORE TRUE)
	ELSE()
		SET(LOO_PLATFORM_NAME "win")
		SET(LOO_PLATFORM_WINDOWS_DESKTOP TRUE)
	ENDIF()
	SET(LOO_PLATFORM_WINDOWS TRUE)
ELSEIF(ANDROID)
	SET(LOO_PLATFORM_NAME "android")
	SET(LOO_PLATFORM_ANDROID TRUE)
ELSEIF(${CMAKE_SYSTEM_NAME} MATCHES "Linux")
	SET(LOO_PLATFORM_NAME "linux")
	SET(LOO_PLATFORM_LINUX TRUE)
ELSEIF(IOS)
	SET(LOO_PLATFORM_NAME "ios")
	SET(LOO_PLATFORM_IOS TRUE)
ELSEIF(${CMAKE_SYSTEM_NAME} MATCHES "Darwin")
	IF(IOS)
		SET(LOO_PLATFORM_NAME "ios")
		SET(LOO_PLATFORM_IOS TRUE)
	ELSE()
		SET(LOO_PLATFORM_NAME "darwin")
		SET(LOO_PLATFORM_DARWIN TRUE)
	ENDIF()
ENDIF()

IF(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Windows")
	SET(LOO_HOST_PLATFORM_NAME "win")
	SET(LOO_HOST_PLATFORM_WINDOWS TRUE)
ELSEIF(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Linux")
	SET(LOO_HOST_PLATFORM_NAME "linux")
	SET(LOO_HOST_PLATFORM_LINUX TRUE)
ELSEIF(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Darwin")
	SET(LOO_HOST_PLATFORM_NAME "darwin")
	SET(LOO_HOST_PLATFORM_DARWIN TRUE)
ENDIF()

IF(NOT LOO_ARCH_NAME)
	IF((CMAKE_SYSTEM_PROCESSOR MATCHES "AMD64") OR (CMAKE_SYSTEM_PROCESSOR MATCHES "x86_64"))
		SET(LOO_ARCH_NAME "x64")
	ELSE()
		SET(LOO_ARCH_NAME "x86")
	ENDIF()
ENDIF()

IF((CMAKE_HOST_SYSTEM_PROCESSOR MATCHES "AMD64") OR (CMAKE_HOST_SYSTEM_PROCESSOR MATCHES "x86_64"))
	SET(LOO_HOST_ARCH_NAME "x64")
ELSE()
	SET(LOO_HOST_ARCH_NAME "x86")
ENDIF()

SET(LOO_PLATFORM_NAME ${LOO_PLATFORM_NAME}_${LOO_ARCH_NAME})
SET(LOO_HOST_PLATFORM_NAME ${LOO_HOST_PLATFORM_NAME}_${LOO_HOST_ARCH_NAME})

IF(LOO_PLATFORM_ANDROID OR LOO_PLATFORM_IOS)
	SET(LOO_PREFERRED_LIB_TYPE "STATIC")
ELSE()
	SET(LOO_PREFERRED_LIB_TYPE "SHARED")
ENDIF()

IF (LOO_PLATFORM_WINDOWS_DESKTOP OR LOO_PLATFORM_LINUX OR LOO_PLATFORM_DARWIN)
	SET(LOO_IS_DEV_PLATFORM TRUE)
ELSE()
	SET(LOO_IS_DEV_PLATFORM FALSE)
ENDIF()
