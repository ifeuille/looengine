project("global")
cmake_minimum_required(VERSION 3.4)
set_property(GLOBAL PROPERTY USE_FOLDERS ON)
file( GLOB_RECURSE SOURCE_PUBLIC "*.*" )


set(MODULE_NAME global)
set(EXTRA_LINKED_LIBRARIES "")
set(MODULE_ROOT_PATH ${CMAKE_CURRENT_SOURCE_DIR})

set(MODULE_ERSION 1)
set(MODULE_SOVERSION 1)
set(FOLDER_PATH Engine/Modules)
if(LOO_PLATFORM_ANDROID)
    find_library(log-lib     log)
    find_library(android-lib    android)
    set(EXTRA_LINKED_LIBRARIES ${EXTRA_LINKED_LIBRARIES}  ${log-lib} ${android-lib})
endif()

source_group( TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${SOURCE_PUBLIC} )
#set_property( TARGET ${MODULE_NAME} PROPERTY FOLDER "" )
#include(${CMAKE_CURRENT_SOURCE_DIR}/include/global/cmake.cmake)
ADD_DEFINITIONS(-DLOOGLOBAL_DLL_EXPORT)
add_dep_glm()
add_static_module()
declare_module_static()
