cmake_minimum_required(VERSION 3.4.1 FATAL_ERROR)

set(ENGINE_ROOT_DIR "res" CACHE PATHS "")
#set(ENGINE_ROOT_DIR "../../../")
file(RELATIVE_PATH ENGINE_ROOT_DIR_TMP ${CMAKE_SOURCE_DIR} ${ENGINE_ROOT_DIR})
message("ENGINE_ROOT_DIR=${ENGINE_ROOT_DIR_TMP}")
set(NAME %SAMPLE_NAME%)
#add_definitions(-DLOO_BUILD_TESTS)
add_definitions(-DLOO_BUILD_TESTS_${SAMPLE_NAME})
add_subdirectory(${ENGINE_ROOT_DIR})